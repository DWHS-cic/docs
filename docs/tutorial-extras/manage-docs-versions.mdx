---
sidebar_position: 1
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# 高中生解題系統

## a

### a001
<Tabs groupId="tutorial-extras">
  <TabItem value="a001-cpp" label="C++">
    ```cpp
    #include <bits/stdc++> //bits/stdc++.h 無法使用時使用下方註解程式碼
    //#include <iostream>
    //#include <string>
    using namespace std;

    int main() {
      string name;
      cin >> name;
      cout << name << ", world" << endl;
      return 0;
    }
    ```
  </TabItem>
  <TabItem value="a001-py" label="Python">
    ```py
    name = input()
    print(name + ", world")
    ```
  </TabItem>
</Tabs>

### a002
<Tabs groupId="tutorial-extras">
  <TabItem value="a002-cpp" label="C++">
    ```cpp
    #include <bits/stdc++> //bits/stdc++.h 無法使用時使用下方註解程式碼
    //#include <iostream>
    using namespace std;

    int main() {
      int a, b;
      cin >> a >> b;
      cout << a + b << endl;
      return 0;
    }
    ```
  </TabItem>
  <TabItem value="a002-py" label="Python">
    ```py
    a, b = map(int, input().split())
    print(a + b)
    ```
  </TabItem>
</Tabs>

### a003
<Tabs groupId="tutorial-extras">
  <TabItem value="a003-cpp" label="C++">
    ```cpp
    #include <bits/stdc++> //bits/stdc++.h 無法使用時使用下方註解程式碼
    //#include <iostream>
    using namespace std;

    int main() {
      int m, d;
      cin >> m >> d;
      s = (m * 2 + d) % 3;
      cout << s << endl;
      return 0;
    }
    ```
  </TabItem>
  <TabItem value="a003-py" label="Python">
    ```py
    m, d = map(int, input().split())
    s = (m * 2 + d) % 3
    print(s)
    ```
  </TabItem>
</Tabs>

### a004
<Tabs groupId="tutorial-extras">
  <TabItem value="a004-cpp" label="C++">
    ```cpp
    #include <bits/stdc++> //bits/stdc++.h 無法使用時使用下方註解程式碼
    //#include <iostream>
    using namespace std;

    int main() {
      int n;
      int i;

      while (cin >> n) {
        if (n % 400 == 0) {
          cout << "閏年" << endl;
        } else if (n % 100 == 0) {
          cout << "平年" << endl;
        } else if (n % 4 == 0) {
          cout << "閏年" << endl;
        } else {
          cout << "平年" << endl;
        }
      }
        

      return 0;
    }
    ```
  </TabItem>
  <TabItem value="a004-py" label="Python">
    ```py
    while True:
      try:
        n = int(input())

        if n % 400 == 0:
          print("閏年")
        elif n % 100 == 0:
          print("平年")
        elif n % 4 == 0:
          print("閏年")
        else:
          print("平年")
      except EOFError:
        break
    ```
  </TabItem>
</Tabs>

### a005
<Tabs groupId="tutorial-extras">
  <TabItem value="a006-cpp" label="C++">
    ```cpp
    #include <bits/stdc++> //bits/stdc++.h 無法使用時使用下方註解程式碼
    //#include <iostream>
    using namespace std;

    void run() {
        int arr[5];
        for (int i = 0; i < 4; i++) {
            cin >> arr[i];
        }
        if (arr[1] - arr[0] == arr[3] - arr[2]) {
            arr[4] = arr[3] + (arr[1] - arr[0]);
        }else if (arr[1] / arr[0] == arr[3] / arr[2]) {
            arr[4] = arr[3] * (arr[1] / arr[0]);
        }
        for (int i = 0; i < 5; i++) {
            cout << arr[i] << " ";
        }
        cout << "\n";
        return;
    }

    int main() {
        int t;
        cin >> t;
        for (int i = 0; i < t; i++) {
            run();
        }
        
        return 0;
    }
    ```
  </TabItem>
  <TabItem value="a005-py" label="Python">
    ```py
    t = int(input())
  for i in range(t):
      arr = list(map(int, input().split()))

      if arr[1] - arr[0] == arr[2] - arr[1]:
          ans = arr[3] + (arr[2] - arr[1])
      elif arr[1] // arr[0] == arr[2] // arr[1]:
          ans = arr[3] * (arr[2] // arr[1])
      print(arr[0], arr[1], arr[2], arr[3], ans)
    ```
  </TabItem>
</Tabs>

### a006
<Tabs groupId="tutorial-extras">
  <TabItem value="a005-cpp" label="C++">
    ```cpp
    #include <bits/stdc++> //bits/stdc++.h 無法使用時使用下方註解程式碼
    //#include <iostream>
    //#include <math.h>
    using namespace std;

    int main() {
        int a, b, c, x, d;
        double x1, x2;
        cin >> a >> b >> c;
        d = (b * b) - (4 * a * c);
        if (d > 0) {
            x1 = (-b + sqrt(d)) / (a * 2);
            x2 = (-b - sqrt(d)) / (a * 2);
            if (x1 < x2) {
                cout << "Two different roots x1=" << x2 << " , x2=" << x1 << "\n";
            } else {
                cout << "Two different roots x1=" << x1 << " , x2=" << x2 << "\n";
            }
        } else if (d == 0) {
            x = -b / (a * 2);
            cout << "Two same roots x=" << x << "\n";
        } else {
            cout << "No real root\n";
        }

        return 0;
    }
    ```
  </TabItem>
  <TabItem value="a006-py" label="Python">
    ```py
    import math
    a, b, c = map(int, input().split())
    d = (b * b) - (4 * a * c)
    if d > 0:
        x1 = (-b + math.sqrt(d)) / (a * 2)
        x2 = (-b - math.sqrt(d)) / (a * 2)
        if x1 < x2:
            print("Two different roots x1=%d , x2=%d" % (x2, x1))
        else:
            print("Two different roots x1=%d , x2=%d" % (x1, x2))
    elif d == 0:
        x = -b / (a * 2)
        print("Two same roots x=%d" % x)
    else:
        print("No real root")
    ```
  </TabItem>
</Tabs>